== Comment lire les résultats d'un fichier BLAST? ==

La procédure pour lire les résultats FASTA est très similaire à celle utiliser pour lire les résultats BLAST. En prenant la recette pour le lecteur BLAST, remplacer la ligne suivante:

<pre>XMLReader parser = new BlastLikeSAXParser();</pre>

par

<pre>XMLReader parser = new FastaSearchSAXParser();</pre>

Vous avez maintenant un parser FASTA fonctionnel ;-)